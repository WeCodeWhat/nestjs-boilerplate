{
  "type": "postgres",
  "host": "localhost",
  "port": 5432,
  "name": "default",
  "database": "todo",
  "synchronize": true,
  "logging": true,
  "entities": ["dist/**/*.entity{.ts,.js}"],
  "migrations": ["dist/migration/**/*.ts"],
  "subscribers": ["dist/subscriber/**/*.ts"]
}
